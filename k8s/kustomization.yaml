apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ff-gemini-bot
resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  # secret.example.yaml намеренно НЕ подключаем — секрет создаётся вручную,
  # чтобы не хранить токены в git. См. README.
